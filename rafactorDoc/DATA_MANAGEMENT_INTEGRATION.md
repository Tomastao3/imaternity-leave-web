# 基础数据管理集成 - 5个子Tab

## 🎯 集成目标

将现有的 `CityDataManager` 组件集成到新的左侧导航栏布局中，保留其5个子Tab功能。

## 📋 5个子Tab列表

| 图标 | 名称 | 组件 | 权限 |
|------|------|------|------|
| 📅 | 产假规则 | MaternityRulesManager | HR |
| 💰 | 津贴规则 | AllowanceRulesManager | HR |
| 💸 | 返还规则 | RefundRulesManager | HR |
| 👥 | 员工信息 | EmployeeInfoManager | HR |
| 🧨 | 节假日 | HolidayManager | HR |

## 🔄 修改内容

### 1. DataManagement.js - 简化为容器

**修改前**:
```javascript
// 自己管理子Tab
const [activeSubTab, setActiveSubTab] = useState('city');
const subTabs = [
  { id: 'city', label: '城市数据管理' },
  { id: 'employee', label: '员工数据管理' }
];
```

**修改后**:
```javascript
// 直接使用CityDataManager
const DataManagement = ({ userRole }) => {
  return (
    <div className="data-management">
      <CityDataManager userRole={userRole} />
    </div>
  );
};
```

### 2. DataManagement.css - 简化样式

**修改前**:
```css
/* 复杂的子Tab样式 */
.sub-tabs { ... }
.sub-tab { ... }
.sub-content { ... }
```

**修改后**:
```css
/* 简单的容器样式 */
.data-management {
  width: 100%;
  height: 100%;
  display: flex;
  flex-direction: column;
  background: #ffffff;
  overflow: hidden;
}
```

## 📐 组件结构

```
MainLayout
  └─ DataManagement (容器)
      └─ CityDataManager (主组件)
          ├─ TabHeader (头部)
          ├─ 城市筛选器
          ├─ 5个子Tab导航
          └─ 子Tab内容
              ├─ MaternityRulesManager (产假规则)
              ├─ AllowanceRulesManager (津贴规则)
              ├─ RefundRulesManager (返还规则)
              ├─ EmployeeInfoManager (员工信息)
              └─ HolidayManager (节假日)
```

## 🎨 界面布局

```
┌─────────────────┬────────────────────────────────────────┐
│                 │                                        │
│  功能列表       │  🏙️ 基础数据管理                       │
│                 │  管理各城市的产假规则、津贴规则...      │
│  💰 产假津贴计算│  筛选城市: [全部城市 ▼]                │
│  📦 批量处理    │                                        │
│  🤖 智能助手    │  ┌──────────────────────────────────┐ │
│  📊 基础数据管理│  │ 📅产假规则 💰津贴规则 💸返还规则  │ │
│     ← 激活      │  │ 👥员工信息 🧨节假日              │ │
│                 │  └──────────────────────────────────┘ │
│                 │                                        │
│                 │  [当前子Tab的内容]                     │
│                 │                                        │
└─────────────────┴────────────────────────────────────────┘
```

## 🔧 技术实现

### 1. 权限控制

```javascript
// 在CityDataManager中
const showEmployeeTab = userRole === 'hr';

const tabs = [
  { id: 'maternity', label: '产假规则', icon: '📅' },
  { id: 'allowance', label: '津贴规则', icon: '💰' },
  { id: 'refund', label: '返还规则', icon: '💸' },
  ...(showEmployeeTab ? [{ id: 'employee', label: '员工信息', icon: '👥' }] : []),
  { id: 'holiday', label: '节假日', icon: '🧨' }
];
```

### 2. 子Tab切换

```javascript
const [activeTab, setActiveTab] = useState('maternity');

<div className="tabs">
  {tabs.map(tab => (
    <button
      key={tab.id}
      className={`tab ${activeTab === tab.id ? 'active' : ''}`}
      onClick={() => setActiveTab(tab.id)}
    >
      <span className="tab-icon">{tab.icon}</span>
      {tab.label}
    </button>
  ))}
</div>
```

### 3. 内容渲染

```javascript
<div className="content-section">
  {activeTab === 'maternity' && (
    <MaternityRulesManager
      selectedCity={selectedCity}
      onDataChange={() => handleDataChange('maternity')}
      onMessage={showMessage}
    />
  )}
  {activeTab === 'allowance' && (
    <AllowanceRulesManager
      selectedCity={selectedCity}
      onDataChange={() => handleDataChange('allowance')}
      onMessage={showMessage}
    />
  )}
  {/* ... 其他子Tab ... */}
</div>
```

## ✅ 功能特点

### 1. 城市筛选
- ✅ 下拉选择城市
- ✅ 支持"全部城市"选项
- ✅ 筛选结果实时更新

### 2. 子Tab导航
- ✅ 5个功能明确的子Tab
- ✅ 图标 + 文字标签
- ✅ 激活状态高亮

### 3. 数据管理
- ✅ 每个子Tab独立管理对应数据
- ✅ 支持增删改查
- ✅ 数据变更实时反馈

### 4. 权限控制
- ✅ 只有HR可以访问
- ✅ 员工信息Tab根据角色显示

## 📝 文件清单

**修改文件**:
1. `src/components/DataManagement.js` - 简化为容器组件
2. `src/components/DataManagement.css` - 简化样式

**使用的现有组件**:
1. `src/components/CityDataManager.js` - 主组件（包含5个子Tab）
2. `src/components/MaternityRulesManager.js` - 产假规则管理
3. `src/components/AllowanceRulesManager.js` - 津贴规则管理
4. `src/components/RefundRulesManager.js` - 返还规则管理
5. `src/components/EmployeeInfoManager.js` - 员工信息管理
6. `src/components/HolidayManager.js` - 节假日管理

**可删除的占位文件**:
1. `src/components/CityDataManagement.js` - 不再需要
2. `src/components/EmployeeDataManagement.js` - 不再需要

## 🎉 集成效果

### 左侧导航栏
```
功能列表
  💰 产假津贴计算
  📦 批量处理
  🤖 智能助手
  📊 基础数据管理  ← 点击后
```

### 右侧内容区域
```
🏙️ 基础数据管理
管理各城市的产假规则、津贴规则、返还规则和员工信息

筛选城市: [全部城市 ▼]

┌─────────────────────────────────────────┐
│ 📅产假规则  💰津贴规则  💸返还规则      │
│ 👥员工信息  🧨节假日                    │
└─────────────────────────────────────────┘

[当前选中的子Tab内容]
```

## 🚀 优势

### 1. 代码复用
- ✅ 直接使用现有的CityDataManager
- ✅ 不需要重写子Tab逻辑
- ✅ 保持原有功能完整

### 2. 结构清晰
- ✅ DataManagement作为简单容器
- ✅ CityDataManager负责具体功能
- ✅ 职责分离明确

### 3. 易于维护
- ✅ 修改子Tab只需改CityDataManager
- ✅ 布局调整只需改DataManagement
- ✅ 互不影响

## 📌 注意事项

1. **权限传递**: 确保 `userRole` 从 `MainLayout` → `DataManagement` → `CityDataManager` 正确传递
2. **样式兼容**: CityDataManager的样式需要适配新布局
3. **数据加载**: 确保城市列表和各类规则数据正常加载

## ✅ 总结

成功将现有的 `CityDataManager`（包含5个子Tab）集成到新的左侧导航栏布局中：

- ✅ 保留了所有5个子Tab功能
- ✅ 简化了DataManagement组件
- ✅ 保持了代码的清晰和可维护性
- ✅ 用户体验保持一致

现在点击左侧导航栏的"基础数据管理"，就能看到完整的5个子Tab页面！🎉
