# 左侧导航栏更新 - 集成所有4个Tab

## 🎯 更新目标

将原有的4个顶部Tab全部移到左侧导航栏，实现统一的侧边栏导航体验。

## 📋 4个Tab列表

| Tab ID | 名称 | 图标 | 权限 |
|--------|------|------|------|
| allowance | 产假津贴计算 | 💰 | 员工、HR |
| batch | 批量处理 | 📦 | HR |
| ai | 智能助手 | 🤖 | 员工、HR |
| data-management | 基础数据管理 | 📊 | HR |

## 🔄 修改内容

### 1. Sidebar.js - 添加所有菜单项

```javascript
const allMenuItems = [
  { id: 'allowance', label: '产假津贴计算', icon: '💰', roles: ['employee', 'hr'] },
  { id: 'batch', label: '批量处理', icon: '📦', roles: ['hr'] },
  { id: 'ai', label: '智能助手', icon: '🤖', roles: ['employee', 'hr'] },
  { id: 'data-management', label: '基础数据管理', icon: '📊', roles: ['hr'] }
];
```

### 2. MainLayout.js - 添加页面渲染

```javascript
import BatchProcessor from './BatchProcessor';
import AIChatDemo from './AIChatDemo';

const renderContent = () => {
  switch (activeTab) {
    case 'allowance':
      return <AllowanceCalculator {...props} />;
    case 'batch':
      return <BatchProcessor />;
    case 'ai':
      return <AIChatDemo />;
    case 'data-management':
      return <DataManagement {...props} />;
  }
};
```

### 3. PageWrapper.css - 确保内容正常显示

```css
.page-wrapper {
  width: 100%;
  min-height: 100vh;
  padding: 0;
  background: #ffffff;
}

.page-wrapper > * {
  max-width: 100%;
  box-sizing: border-box;
}
```

### 4. MainLayout.css - 优化布局

```css
.main-content {
  flex: 1;
  margin-left: 160px;
  overflow-y: auto;
  overflow-x: hidden;
  background: #ffffff;
  padding: 0;
  width: calc(100% - 160px);
  min-height: 100vh;
}
```

## 📐 布局结构

```
┌─────────────────┬────────────────────────────────────┐
│                 │                                    │
│  功能列表       │         主内容区域                  │
│                 │                                    │
│  💰 产假津贴计算│    ┌────────────────────────┐     │
│  📦 批量处理    │    │                        │     │
│  🤖 智能助手    │    │    当前页面内容         │     │
│  📊 基础数据管理│    │                        │     │
│                 │    └────────────────────────┘     │
│                 │                                    │
└─────────────────┴────────────────────────────────────┘
```

## 🎨 用户角色权限

### 员工 (employee)
左侧导航栏显示：
- ✅ 💰 产假津贴计算
- ✅ 🤖 智能助手

### HR (hr)
左侧导航栏显示：
- ✅ 💰 产假津贴计算
- ✅ 📦 批量处理
- ✅ 🤖 智能助手
- ✅ 📊 基础数据管理

## 🔧 技术实现

### 1. 响应式布局

```css
/* 左侧固定宽度 */
.sidebar {
  width: 160px;
  position: fixed;
  left: 0;
  top: 0;
  height: 100vh;
}

/* 右侧自适应 */
.main-content {
  margin-left: 160px;
  width: calc(100% - 160px);
}
```

### 2. 权限过滤

```javascript
const menuItems = useMemo(() => {
  return allMenuItems.filter(item => item.roles.includes(userRole));
}, [userRole]);
```

### 3. 页面包装

```javascript
<div className="page-wrapper">
  {renderContent()}
</div>
```

确保所有页面内容：
- ✅ 不会溢出
- ✅ 正确显示
- ✅ 自适应宽度

## ✅ 优势

### 1. 统一体验
- ✅ 所有功能都在左侧导航栏
- ✅ 不再需要顶部Tab
- ✅ 导航更直观

### 2. 空间利用
- ✅ 右侧内容区域更宽
- ✅ 更好的阅读体验
- ✅ 更多的操作空间

### 3. 可扩展性
- ✅ 轻松添加新功能
- ✅ 灵活的权限控制
- ✅ 清晰的代码结构

## 📝 文件清单

**修改文件**:
1. `src/components/Sidebar.js` - 添加4个菜单项
2. `src/components/MainLayout.js` - 添加页面渲染逻辑
3. `src/components/MainLayout.css` - 优化布局样式

**新增文件**:
1. `src/components/PageWrapper.css` - 页面包装器样式

## 🎉 完成效果

现在所有4个Tab都在左侧导航栏中：

**员工登录看到**:
```
功能列表
  💰 产假津贴计算  ← 激活
  🤖 智能助手
```

**HR登录看到**:
```
功能列表
  💰 产假津贴计算  ← 激活
  📦 批量处理
  🤖 智能助手
  📊 基础数据管理
```

右侧内容区域自适应显示，确保所有页面都能正常工作！
